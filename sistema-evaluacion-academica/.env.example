# URL del backend — copiar este archivo como .env.local y ajustar si es necesario
NEXT_PUBLIC_API_URL=http://localhost:5000

# Opcional: URL del backend usada por las pruebas E2E de Playwright
# PLAYWRIGHT_API_URL=http://localhost:5000
